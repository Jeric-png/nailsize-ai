# LEGACY ML/GCP PROTOTYPE ONLY — not read by the active guided web application.
DEPLOYMENT_ENVIRONMENT=production
VITE_INFERENCE_API_URL=https://api.nailsize.example
ALLOWED_ORIGINS=https://nailsize.example
MODEL_VERSION=replace-with-immutable-model-version
MODEL_SHA256=replace-with-64-character-model-checksum
