# Synthetic image fixtures

These 64×48 images contain only generated color blocks. They exercise local
browser decoding for JPEG, PNG, WebP, HEIC, AVIF, GIF, and BMP without using
customer media. Tests must never treat their successful decode as evidence of
nail-sizing accuracy.
